# Amazon 用户评论爬虫 — 使用说明


## 1. 软件简介

本工具用于爬取 Amazon 用户概况页（Profile）上的全部商品评论，并自动分析是否存在刷评嫌疑。

**核心功能：**

- 从 Amazon 用户概况页自动爬取所有评论（含评分、日期、VP 状态等详情）
- 支持批量输入多个用户 URL，一次爬取多人
- 输出专业排版的 Excel 报告
- 自动生成刷评分析报告（6 项规则打分判定）
- 同时输出 JSON 原始数据，便于二次处理
- 提供图形界面操作方式

---

## 2. 安装与运行

1. 获取 `AmazonReviewScraper` 文件夹（由开发者打包提供）
2. 双击文件夹中的 **`AmazonReviewScraper.exe`** 即可启动
3. 首次运行会自动在同目录下创建 `output`、`browser_data` 等文件夹

---

## 3. GUI 图形界面操作

启动后会看到如下界面：

![image-20260420182221178](C:\Users\msw\AppData\Roaming\Typora\typora-user-images\image-20260420182221178.png)

### 3.1 第一步：登录 Amazon

> **为什么要登录？** 登录后可以看到更多评论详情（如 Verified Purchase 状态），且不容易触发反爬验证。

1. 点击 **「登录 Amazon」** 按钮
2. 程序会自动打开一个 Chromium 浏览器窗口，跳转到 Amazon 登录页
3. 在浏览器中 **手动输入账号密码完成登录**
4. 登录成功后（看到 Amazon 首页），回到程序窗口点击 **「确定」**
5. 登录状态显示为 ✅ 已登录

> 💡 登录状态会保存在 `browser_data` 文件夹中，下次运行无需重新登录。
>
> ![image-20260420182403807](C:\Users\msw\AppData\Roaming\Typora\typora-user-images\image-20260420182403807.png)

### 3.2 第二步：获取用户概况页 URL

在 Amazon 上找到目标用户的概况页，URL 格式如下：

```
https://www.amazon.com/gp/profile/amzn1.account.XXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

**如何找到用户概况页：**

1. 在任意商品评论区，点击评论者的 **用户名**
2. 进入用户概况页后，复制浏览器地址栏的 URL

![image-20260420182509234](C:\Users\msw\AppData\Roaming\Typora\typora-user-images\image-20260420182509234.png)

### 3.3 第三步：开始爬取

1. 在 URL 输入框中粘贴用户概况页 URL（**支持多个，每行一个**）
2. 选择是否勾选 **「无头模式」**：
   - **不勾选（默认）**：会弹出浏览器窗口，可以看到爬取过程，遇到验证码时方便手动处理
   - **勾选**：浏览器在后台运行，不弹出窗口，速度更快但无法手动处理验证码
3. 点击 **「开始爬取」**
4. 在日志区域观察进度，进度条会显示当前进度和预计剩余时间

![image-20260420182633606](C:\Users\msw\AppData\Roaming\Typora\typora-user-images\image-20260420182633606.png)

### 3.4 处理验证码

爬取过程中如果 Amazon 弹出人机验证（验证码）：

1. 程序会自动暂停并弹出提示框
2. 切换到浏览器窗口，**手动完成验证**（拖动滑块、输入字符等）
3. 验证完成后，回到程序点击 **「确定」** 继续爬取

### 3.5 修改设置

点击左下角 **「⚙ 设置」** 可调整以下参数（详见[第 6 节](#6-配置参数详解)）。

点击 **「重置配置」** 可恢复所有参数为默认值。

![image-20260420182730433](C:\Users\msw\AppData\Roaming\Typora\typora-user-images\image-20260420182730433.png)

---

## 4. 输出文件说明

所有输出文件保存在程序目录下的 **`output/`** 文件夹中。批量爬取时生成一个综合 Excel 文件：

```
output/
  └── batch_20260420_153000.xlsx
```

Excel 文件包含多个 Sheet：

| Sheet | 内容 |
|-------|------|
| 用户名 | 该用户的所有评论数据 |
| 用户名_1 | 第二个用户的评论（如有） |
| ... | 更多用户 |
| **分析报告** | 所有用户的刷评分析汇总 |

**评论 Sheet 样式说明：**

| 特性 | 说明 |
|------|------|
| 蓝色表头 | 白色加粗字体，居中对齐 |
| 斑马纹 | 奇偶行交替浅蓝底色，便于阅读 |
| Rating 着色 | 5★ 绿色背景、4★ 浅绿、3★ 浅黄、2★ 浅橙、1★ 浅红 |
| VP 着色 | Yes = 绿色，No = 红色 |
| 自动换行 | Product Name、Title、Description 列自动换行 |
| 冻结首行 | 向下滚动时表头始终可见 |
| 自动筛选 | 点击表头下拉箭头可按条件筛选/排序 |

### 4.1 各列含义

| 列名 | 说明 |
|------|------|
| Review ID | Amazon 评论唯一 ID（如 R1234567890） |
| Product Name | 评论的商品名称 |
| ASIN | Amazon 商品编号（10 位） |
| Title | 评论标题 |
| Description | 评论正文 |
| Rating | 评分（1-5 星） |
| Review Date | 评论发布日期 |
| Verified Purchase | 是否为已验证购买（Yes/No） |
| Helpful Votes | 被其他用户标记为 "有帮助" 的次数 |
| Hearts | 被点赞次数 |
| Review URL | 评论详情页链接 |

---

## 5. 配置参数详解

配置保存在程序目录下的 `config.json` 文件中。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `headless` | `false` | 默认无头模式。`false` = 弹出浏览器窗口；`true` = 后台运行 |
| `page_timeout` | `90000` | 概况页加载超时（毫秒）。网络慢时可适当增大 |
| `detail_timeout` | `30000` | 评论详情页加载超时（毫秒） |
| `delay_min` | `4` | 每条评论之间的最小等待时间（秒） |
| `delay_max` | `8` | 每条评论之间的最大等待时间（秒） |
| `browse_min` | `2` | 详情页模拟浏览最小时间（秒） |
| `browse_max` | `5` | 详情页模拟浏览最大时间（秒） |
| `max_scrolls` | `100` | 概况页最大滚动次数。评论很多时需增大 |
| `max_no_change` | `5` | 连续滚动无新评论时停止的次数 |
| `user_agent` | `""` | 自定义 User-Agent。留空使用浏览器默认值 |

> ⚠️ **反爬建议：** `delay_min` 和 `delay_max` 不要设置太小，建议保持 4-8 秒。过快访问容易触发 Amazon 的反爬机制，导致 IP 被封或频繁出现验证码。

---

## 6. 刷评分析报告解读

分析报告 Sheet 会对每个用户的评论行为进行打分，判断是否存在刷评嫌疑。

### 6.1 评分规则

| 规则条件 | 分值 | 说明 |
|----------|------|------|
| 5 星评价占比 > 80% | +2 分 | 正常用户很少全部给好评 |
| 已验证购买占比 < 40% | +2 分 | 大量评论没有购买记录，很可疑 |
| 评论平均长度 < 80 字符 | +1 分 | 内容空洞，可能是模板评论 |
| 同一天发布 ≥ 3 条评论 | +2 分 | 短时间大量发布不正常 |
| 所有评论无人点赞（≥5 条时） | +1 分 | 正常评论通常会有互动 |
| 评论总数 > 50 条 | +1 分 | 活跃度异常高 |

### 6.2 结论判定

| 总分 | 结论 | 含义 |
|------|------|------|
| 0-2 分 | ✅ 未发现明显异常 | 评论行为正常 |
| 3-4 分 | ⚠️ 存在刷评嫌疑 | 有可疑迹象，建议人工复核 |
| ≥ 5 分 | 🚨 高度疑似刷评 | 多项指标异常，高概率为刷评用户 |

### 6.3 报告样式

- **可疑评分和结论**按严重程度着色：绿色（正常）、黄色（嫌疑）、红色（高度疑似）
- 报告下方附有完整的评分规则说明表格
- 支持冻结首行和自动筛选

---

## 7. 常见问题

### Q: 爬取时出现验证码怎么办？

确保使用有头模式（不勾选「无头模式」），程序会自动暂停并提示你在浏览器中手动完成验证。

### Q: 登录状态过期了怎么办？

重新点击「登录 Amazon」，在浏览器中重新登录即可。登录状态保存在 `browser_data` 文件夹中。

### Q: 概况页评论加载不全怎么办？

在设置中增大 `max_scrolls`（最大滚动次数）和 `max_no_change`（无新数据重试次数）。

### Q: 爬取速度太慢了怎么办？

可以适当减小 `delay_min` 和 `delay_max`，但 **不建议低于 2 秒**，否则容易触发反爬机制。

### Q: 某个用户爬取失败但其他用户成功了？

批量爬取时，单个用户失败不影响其他用户。失败的用户会在日志中显示错误原因，成功的用户仍会正常生成报告。

### Q: 打包后的 exe 打开没反应？

- 确保没有被杀毒软件拦截（PyInstaller 打包的 exe 可能被误报）
- 尝试右键「以管理员身份运行」
- 检查 `AmazonReviewScraper` 文件夹是否完整（不要只拷贝 exe 文件）

### Q: 如何删除登录状态？

删除程序目录下的 `browser_data` 文件夹即可清除所有浏览器数据和登录状态。

### Q: 程序支持哪些 Amazon 站点？

目前支持 Amazon.com（美国站）。用户概况页 URL 必须包含 `amazon.com/gp/profile/`。
